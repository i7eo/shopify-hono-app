import { name } from "../../package.json";

export const DEFAULT_APP_NAME = name.split("/")[0];

export const DEFAULT_APP_GLOBAL_API_PREFIX = "api";

export const DEFAULT_APP_LOCALE = "en";

export const DEFAULT_APP_USE_CLUSTER = false;
