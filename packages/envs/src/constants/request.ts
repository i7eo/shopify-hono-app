export const DEFAULT_REQUEST_TIMEOUT = 1000 * 60 * 3; // limit request timeout to 3 minutes
export const DEFAULT_UPLOAD_TIMEOUT = 1000 * 60 * 5; // limit upload timeout to 5 minutes
export const DEFAULT_UPLOAD_MAX_SIZE = 1024 * 200; // limit size 200kb
